# Lab 7

My name: Anish Kondamadugula

## Check Your Understanding

### 1.
I would fit the automated tests in the Recipe project development pipeline wihin a GitHub Action that runs whenever code is pushed. This is because this allows the automated tests catch bugs every time new code is added. It is beter than just runnin gtests manually because it helps prevent broken code from being merged or deployed.

### 2.
I would not use an end to end test to check if a function is returning the correct output because an end to end test is for teasting a full user workflow, and not just a singular function output. In this case, a unit test would be a better fit.

### 3.
Navigation mode tests the page from the beginning of a normal page load and provides an overall performance report. Meanwhile, snapshot mode analyzes the page in its current state, which is better for checking accessibility issues but doesn't measure the full loading process.

### 4.
The site can be improved with the addition of a lang attribute to the <html> tag, adding a meta description for SEO, and reducing unused JavaScript.
